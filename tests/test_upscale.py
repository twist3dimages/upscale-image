import pytest
from upscale import upscale_images_in_dir

def test_upscale_images_in_dir():
    # Placeholder test to verify script execution without errors
    upscale_images_in_dir(None, None, None)
    assert True

if __name__ == '__main__':
    pytest.main(['-v', '-s'])
